# Handoff: Backend → TASK-007

## Статус: DONE ✅

## Что сделано

Изменён `deployments/docker-compose.yaml` — все внутренние порты микросервисов привязаны к `127.0.0.1`:

| Сервис | Было | Стало |
|--------|------|-------|
| order-svc | `"9001:9001"` | `"127.0.0.1:9001:9001"` |
| marketdata-svc | `"9002:9002"` | `"127.0.0.1:9002:9002"` |
| portfolio-svc | `"9003:9003"` | `"127.0.0.1:9003:9003"` |
| risk-svc | `"9004:9004"` | `"127.0.0.1:9004:9004"` |
| prometheus | `"9090:9090"` | `"127.0.0.1:9090:9090"` |

Gateway (`:18080:8080`) остаётся без изменений — он уже забинден на loopback и доступен через nginx.

## Почему безопасно

- Docker networking позволяет контейнерам общаться через внутреннюю сеть `trading-bot-net` по имени сервиса (order-svc, marketdata-svc и т.д.) — это не зависит от host-bind
- Gateway обращается к микросервисам по имени контейнера через Docker DNS, а не по localhost
- Порты `expose` (9101–9104) остаются без изменений — они для межконтейнерного доступа

## Артефакты

- `artifacts/docker-compose.patch` — git diff

## Корректировки для DevOps

Деплой: применить compose, проверить healthcheck и доступность портов извне.

## Блокеры: НЕТ